<!DOCTYPE html>
<html lang="en">
  <head>  
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-do List</title>
    <link rel="stylesheet" href="/home.css"> 
  </head>
  <body>
    <div class="header-container">
        <h1>To-do List</h1>
        <h1>Welcome, <%= email %>!</h1>
        <div class="tabButtons" id="tabButtonsDiv">
          <button id="homeButton"">Home</button>
          <button id="historyButton">History</button>

          <form action="/logout" method="POST">
            <button id="logoutButton" type="submit">Logout</button>
          </form>

        </div>
      </div>

      <!-- Helps format the buttons better -->
      <style>
        .header-container{
          padding:20px;
        }

       .tabButtons{
        display:flex;
       } 
      </style>

      
      <!-- Add Button -->
      <div class='task-group' id="group">
        <input id="taskInputAdd" class="taskInputAdd" placeholder="Enter Task">
        <button id="taskButtonAdd" type="button">+</button>
      </div>

      <!-- Displays the textbox after pressing the "+" button -->
      <div class="task-list" id="taskContainer">
        <div class="textarea-container">
          <br>
        </div>
      </div>
      

      <script src="../public/home.js"></script>
      <script src="../public/popUp.js"></script> 
  </body>
</html>
